<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Ruby | Jason's space]]></title>
  <link href="http://qjpcpu.github.io/blog/categories/ruby/atom.xml" rel="self"/>
  <link href="http://qjpcpu.github.io/"/>
  <updated>2014-02-14T10:34:28+08:00</updated>
  <id>http://qjpcpu.github.io/</id>
  <author>
    <name><![CDATA[Jason]]></name>
    <email><![CDATA[qjpcpu@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[创建自己的gem]]></title>
    <link href="http://qjpcpu.github.io/blog/2014/02/14/chuang-jian-zi-ji-de-gem/"/>
    <updated>2014-02-14T09:45:52+08:00</updated>
    <id>http://qjpcpu.github.io/blog/2014/02/14/chuang-jian-zi-ji-de-gem</id>
    <content type="html"><![CDATA[<p>如果在开发过程中遇到比较通用化的场景，并且估计这种情况其他开发者也可能遇到，就可以把解决方案做成ruby gem，放到<a href="https://rubygems.org">rubygems.org</a>上供所有人使用，并且将源码托管到github上使得解决方案逐步成熟。</p>

<p>以创建一个名为jgem的gem为例，下面就是创建gem的具体步骤：</p>

<ol>
<li>首先要有<a href="https://rubygems.org">rubygems.org</a>和<a href="https://github.com">github</a>的帐号。</li>
<li><p>利用bundler创建新的gem：</p>

<pre><code> $ bundle gem jgem
       create  jgem/Gemfile
       create  jgem/Rakefile
       create  jgem/LICENSE.txt
       create  jgem/README.md
       create  jgem/.gitignore
       create  jgem/jgem.gemspec
       create  jgem/lib/jgem.rb
       create  jgem/lib/jgem/version.rb
 Initializing git repo in /Users/jason/Documents/tmp/jgem
</code></pre></li>
<li><p>修改<code>.gemspec</code>文件，主要修改新gem描述：</p>

<pre><code> spec.summary       = %q{TODO: Write a short summary. Required.}
 spec.description   = %q{TODO: Write a longer description. Optional.}
</code></pre>

<p> 添加gem的依赖，包括开发时依赖和运行时依赖：</p>

<pre><code> spec.add_development_dependency "rake"
 spec.add_runtime_dependency "activerecord"
</code></pre></li>
<li><p>编译gem</p>

<pre><code> $ gem build jgem.gemspec 
 WARNING:  no homepage specified
 WARNING:  open-ended dependency on rake (&gt;= 0, development) is not recommended
   if rake is semantically versioned, use:
     add_development_dependency 'rake', '~&gt; 0'
 WARNING:  See http://guides.rubygems.org/specification-reference/ for help
   Successfully built RubyGem
   Name: jgem
   Version: 0.0.1
   File: jgem-0.0.1.gem
</code></pre>

<p> 编译后生成jgem-0.0.1.gem。</p>

<p> 注意：在每次<code>gem build</code>编译gem前，必须<code>git add .</code>或<code>git commit</code>一次，因为由于gem和git的紧密关系，没有<code>git add</code>到暂存区的文件变更很可能不会被编译进gem。有的朋友说在bin目录下创建的可执行文件始终无法被编译进gem就是这个原因（他们采取的办法是将<code>.gemspec</code>文件的<code>spec.executables   = spec.files.grep(%r{^bin/}) { |f| File.basename(f) }</code>改成<code>spec.executables =['your_executable_file']</code>，实际这是不必要的）</p></li>
<li><p>发布</p>

<p> 发布生成的gem到rubygems.org。</p>

<pre><code> $ gem push jgem-0.0.1.gem
</code></pre>

<p> 根据提示输入用户信息就行了。</p></li>
<li><p>托管代码</p>

<p>在github上新建一个代码库，名称最好和gem保持一致为jgem。</p>

<pre><code> git remote add origin git@github.com:USERNAME/jgem.git
 git push -u origin master
</code></pre></li>
<li><p>更新gem代码</p>

<p>如果在本地更新代码，需要先<code>git add .</code>或<code>git commit</code>，然后执行<code>gem build</code>才能正确编译。编译好的gem可以先在本地安装试用：</p>

<pre><code>$ gem build jgem.gemspec
$ gem install --local jgem-0.0.1.gem
</code></pre>

<p>如果要上传到rubygems，则必须更新<code>jgem/lib/jgem/version.rb</code>里的版本号，相同的版本号是不允许重复上传的。到rubygems上传的只是编译好的gem，不要忘了提交代码变更到github。</p>

<p>PS. 如果gem需要添加可执行文件如<code>example_cmd</code>，在jgem目录下创建bin目录来放置这些命令即可(反复提醒，别忘了将新文件添加到git库哦)。</p>

<pre><code>$ mkdir bin
$ touch bin/example_cmd
$ chmod a+x bin/example_cmd
</code></pre></li>
</ol>

]]></content>
  </entry>
  
</feed>
